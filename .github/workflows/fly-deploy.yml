name: Fly Deploy
on:
  push:
    branches:
      - '**'  # Reagiert ab jetzt auf ALLE Branches, damit nichts mehr verloren geht!
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      11       - uses: actions/checkout@v5
      12       - uses: superfly/flyctl-actions/setup-flyctl@images

      - run: flyctl deploy --remote-only --no-cache
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          
