name = "securechat-media"
main = "media-worker.js"
compatibility_date = "2026-01-01"

# Kostenloses Kontingent: 10 GB Speicher, 10 Mio. Class-A-Operationen,
# 1 Mio. Class-B-Operationen pro Monat — siehe FREE-STACK.md.
[[r2_buckets]]
binding = "MEDIA"
bucket_name = "securechat-media"

# Für Sitzungs-Cache und Kontingent-Zähler. Auch im Free-Plan enthalten.
[[kv_namespaces]]
binding = "SESSIONS"
id = "<wird durch: wrangler kv namespace create SESSIONS erzeugt>"

[vars]
# Wohin der Worker Bearer-Token zur Prüfung schickt — dein bestehender
# /api/me-Endpunkt tut das bereits.
AUTH_CHECK_URL = "https://chat.example.com/api/me"
QUOTA_BYTES = "52428800"
ALLOWED_ORIGIN = "https://chat.example.com"
